import styled from 'styled-components'
import {
  space,
  layout,
  color,
  typography,
  flexbox,
  grid,
  background,
  border,
  position,
  shadow,
} from 'styled-system'

const Box = styled.div`
${space}
${layout}
${color}
${typography}
${flexbox}
${grid}
${background}
${border}
${position}
${shadow}
`

export default Box
