import React from 'react'
import PropTypes from 'prop-types'

const AppWrap = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  )
}

AppWrap.propTypes = {
  
}

export default AppWrap